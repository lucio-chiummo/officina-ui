/**
 * @officina/ui — the complete Officina component library.
 *
 * A self-contained set of 250+ React primitives, token CSS, and composable
 * components. tsup bundles the primitive source (aliases resolved, peer/runtime
 * deps externalized) so consumers get every component from a single install.
 */
export * from '@primitives';
export * from '@hooks';
