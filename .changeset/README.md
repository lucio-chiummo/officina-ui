# Changesets

This folder is managed by [changesets](https://github.com/changesets/changesets).

Run `pnpm changeset` to record a change, then the Release workflow opens a
"Version Packages" PR. Merging it versions `@officina/ui` and publishes to npm.
