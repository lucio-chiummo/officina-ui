node_modules
dist
out
drift-manifest.json
.next
apps/docs/.source
# Regenerated by every `next build`, and gitignored — formatting it makes
# format:check fail on any machine that has built the docs.
apps/docs/next-env.d.ts
pnpm-lock.yaml
