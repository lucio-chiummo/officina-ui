---
title: List
description: Vertical container for stacked rows — the root of the List composition.
---

import { ListDemo } from '@/components/demos/data-display-demo';

`List` is the styled `<ul>` that wraps a stack of `ListItem` rows. It owns the
shared spacing and surface — pass `variant="outlined"` or `"soft"` for a bordered
or filled container, and `dense` to tighten the row gap. Fill it with `ListItem`,
`ListSubheader`, and `ListDivider` children to build navigation menus, settings
panels, and entity lists.

## Example

<Demo>
  <ListDemo />
</Demo>

```tsx
import { List, ListItem, ListItemButton } from '@officina/ui';

<List variant="outlined">
  <ListItem>
    <ListItemButton selected>Dashboard</ListItemButton>
  </ListItem>
  <ListItem>
    <ListItemButton>Settings</ListItemButton>
  </ListItem>
</List>;
```

## API

### List

The styled `<ul>` container.

<PropsTable name="List" />

### ListItem

A single row inside a `List` — wraps content or a `ListItemButton`.

<PropsTable name="ListItem" />

### ListItemButton

Interactive, focusable row button with a selected state, for menus and nav.

<PropsTable name="ListItemButton" />

### ListItemContent

Title plus muted description block for a list row, with truncation.

<PropsTable name="ListItemContent" />

### ListItemDecorator

Fixed-size leading slot for an icon or avatar in a list row.

<PropsTable name="ListItemDecorator" />

### ListSubheader

Small uppercase caption that labels a group of rows within a `List`.

<PropsTable name="ListSubheader" />

### ListDivider

A horizontal separator rule between groups of list rows.

<PropsTable name="ListDivider" />
